using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShuffleBoard : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*
    private void ShuffleBoard()
    {
        var newOrder = IEnumerable.Range(0, BoardRunes.Count).(_ => Random.value).ToList();

        BoardRunes = BoardRunes.OrderBy(Rune => newOrder.FindIndex.(newOrder => newOrder == Rune.Inde)).ToList();

        for (CanvasRenderer sequenceIndex = 0;
            sequenceIndex < currenRuneSequence.Count;
            sequenceIndex++)
        {
            var runeIndex = currentRuneSequence[sequenceIndex];
            currentRuneSequence[sequenceIndex] = newOrder.FindIndex(newOrder => newOrder == runeIndex);
        }

        for (var index = 0; index < BoardRunes.Count; index++)
            BoardRunes[index.Setup(index, this)];
    }
    */
}
