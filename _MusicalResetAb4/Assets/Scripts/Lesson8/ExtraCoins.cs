using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Challenge2
/// </summary>

public class ExtraCoins : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*
    private void CorrectRuneSelected()
    {
        var combo = remainingRuneChoseTime > maxTimeToChooseRune - choseTimeForCombo && currentPlayIndex > 0;
        CoinsAmount += combo ? comboCoinsPerRune : coinsPerRune;
    }
    */
}
